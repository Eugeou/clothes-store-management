export type Branch = {
    // Id: string;
    // BrandName: string;
    // Description: string;
    // ImageSource: string;
    id: string;
    name: string;
};

export type CreatedBrand = {
    BrandName: string;
    Description: string;
    ImageSource: File;
}